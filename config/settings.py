# settings.py

# Default version for the UI
APP_VERSION = "v1.0.0"

# Default compression quality
DEFAULT_QUALITY = 75

# Supported output formats
SUPPORTED_FORMATS = ["JPEG", "PNG", "WEBP"]

# Default output directory (optional)
DEFAULT_OUTPUT_DIR = "output"

# Default image size (optional)
DEFAULT_WIDTH = None
DEFAULT_HEIGHT = None

# UI settings
APP_TITLE = "PixelForge – Image Tools"
WINDOW_SIZE = (550, 550)
